class Solution {
    public int andInRange(int l, int r) {
        int shift = 0;
        
        // Find the common prefix
        while (l < r) {
            l >>= 1;
            r >>= 1;
            shift++;
        }
        
        // Shift back to get result
        return l << shift;
    }
}
