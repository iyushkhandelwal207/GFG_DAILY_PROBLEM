import java.util.Queue;

class Solution {
    public void reverseQueue(Queue<Integer> q) {
        if (q.isEmpty()) {
            return;
        }
        int front = q.poll();          // remove front element
        reverseQueue(q);               // reverse remaining queue
        q.offer(front);                // add removed element at end
    }
}
